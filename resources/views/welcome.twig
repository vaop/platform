{#
 # Welcome page
 #}
{% extends 'layouts/guest.twig' %}

{% block title %}{{ t('Welcome') }} - {{ app.name }}{% endblock %}

{% block content %}
<div class="center-content section-lg">
    <div class="container container-lg" style="text-align: center;">
        <h1 class="heading-2xl">
            {{ t('Welcome to') }}
            <span style="color: var(--color-primary);">{{ app.name }}</span>
        </h1>
        <p class="text-lead text-muted" style="max-width: 48rem; margin: 1rem auto;">
            {{ t('Your virtual airline operations platform.') }}
        </p>
        <div class="cluster cluster-4" style="justify-content: center; margin-top: 2rem;">
            {% if is_guest() %}
                {% include 'components/button.twig' with {
                    href: url('/login'),
                    label: t('Log In'),
                    variant: 'primary',
                    size: 'lg'
                } %}
                {% include 'components/button.twig' with {
                    href: url('/register'),
                    label: t('Register'),
                    variant: 'secondary',
                    size: 'lg'
                } %}
            {% else %}
                {% include 'components/button.twig' with {
                    href: url('/dashboard'),
                    label: t('Go to Dashboard'),
                    variant: 'primary',
                    size: 'lg'
                } %}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
